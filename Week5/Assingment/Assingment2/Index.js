class pet {
    constructor(name,type){
        this.name  = name; 
        this.type = type;
    }
    getdes() {
        return this; `Hello, my name is ${this.name} and i am a ${this.type}`
    }
}
